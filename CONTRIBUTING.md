# Contributing

Run `./tools/verify.ps1 -Unit` before submitting changes. Keep contracts versioned, add regression coverage for behavior changes, and do not commit generated outputs, caches, logs, or proprietary inputs. New tracked assets require the provenance record described in `docs/ASSET_PROVENANCE.md`.
